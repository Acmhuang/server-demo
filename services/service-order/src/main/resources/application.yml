server:
  port: 8000
order:
  timeout: 21min
  auto-confirm: 3d
spring:
  profiles:
    active: test
    include: feign
  application:
    name: service-order
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        namespace: ${spring.profiles.active}
        group: order
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
logging:
  level:
    com.acmhuang.order.feign: debug
---
spring:
  config:
    import:
      - nacos:common.yml
      - nacos:database.yml
    activate:
      on-profile: dev

---
spring:
  config:
    import:
      - nacos:common.yml
      - nacos:database.yml
    activate:
      on-profile: prod

---
spring:
  config:
    import:
      - nacos:common.yml
    activate:
      on-profile: test
